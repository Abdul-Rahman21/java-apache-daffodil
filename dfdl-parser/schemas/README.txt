Place IBM ACE DFDL schema files (XSD) in this directory.

They are mounted into the container at /app/schema.

Default primary schema path:
  /app/schema/CYO_SMPREQ.xsd

Include all xs:import / xs:include dependencies here as well,
preserving relative schemaLocation paths used by your ACE project.
